/*const db=require("./database");
const performAction=require("./test");
const personId = 19; // Specify the person ID you want to check
performAction.performAction(personId,1); */
const performAction = require('./test'); // Adjust the path accordingly

// Example usage
const personId = 20; // Specify the person ID you want to check
const cnst =1054251; // Specify the value of 'cnst'
performAction.performAction(personId, cnst);
